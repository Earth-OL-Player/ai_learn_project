## 1. 后端依赖与配置

- [x] 1.1 在 `ai-learn-backend` 中补充 MySQL、Flyway、MyBatis 或项目选定持久化方案相关依赖
- [x] 1.2 在后端配置中新增数据库连接、Flyway、JWT 密钥和 token 过期时间占位配置，确保不提交真实敏感信息
- [x] 1.3 新增或调整统一错误码，覆盖 `AUTH_UNAUTHORIZED`、`RESOURCE_CONFLICT` 和参数校验失败场景
- [x] 1.4 核对统一响应、traceId 和异常处理逻辑，确保认证接口和用户接口均返回统一响应结构

## 2. 数据库迁移与中间件文档

- [x] 2.1 新增 Flyway 迁移脚本 `V1__init_user_tables.sql`，创建 `users` 表及用户名、邮箱唯一约束
- [x] 2.2 确认 `users` 表字段覆盖用户 ID、用户名、昵称、头像、邮箱、密码哈希、经验值、等级、段位和审计字段
- [x] 2.3 更新 `doc/中间件/MySQL.md`，补充用途、推荐版本、本地安装、本地启动、必要配置、占位符示例、验证方式和部署注意事项
- [x] 2.4 在 README 或启动说明中补充本期 MySQL 准备、后端启动和接口联调入口说明

## 3. 后端用户与认证能力

- [x] 3.1 按 `auth`、`user`、`common/security` 分层创建后端包结构和必要类
- [x] 3.2 实现用户领域模型、用户摘要 DTO、注册请求 DTO、登录请求 DTO 和认证响应 DTO
- [x] 3.3 实现用户仓储能力，支持按用户名查询、按邮箱查询、按 ID 查询和创建用户
- [x] 3.4 实现密码哈希与校验能力，确保数据库仅保存 BCrypt 或 Argon2 哈希
- [x] 3.5 实现 JWT 签发、解析、过期校验和 Bearer token 提取能力
- [x] 3.6 实现注册应用服务，覆盖参数校验、用户名唯一、邮箱唯一、默认等级段位和签发 token
- [x] 3.7 实现登录应用服务，覆盖用户名不存在、密码错误和登录成功签发 token
- [x] 3.8 实现当前用户查询能力，确保响应不包含 `password` 或 `passwordHash`
- [x] 3.9 实现退出登录接口，采用无状态 JWT 模式返回统一成功响应
- [x] 3.10 配置认证过滤器或拦截器，保护 `/api/v1/users/me` 和 `/api/v1/auth/logout`，放行注册、登录、健康检查等游客接口

## 4. 前端登录态与接口封装

- [x] 4.1 新增 `src/api/auth.ts`，封装注册、登录和退出登录接口调用
- [x] 4.2 新增 `src/api/user.ts`，封装 `/users/me` 当前用户接口调用
- [x] 4.3 新增或完善前端请求拦截逻辑，为已登录请求追加 `Authorization: Bearer <token>` 请求头
- [x] 4.4 新增 `src/stores/auth.ts`，用 Pinia 管理 `token`、`user`、`initialized` 和本地存储 key `ai_learn_access_token`
- [x] 4.5 实现刷新后通过 `/users/me` 恢复登录态，恢复失败时清理 token 和用户信息

## 5. 前端页面与交互

- [x] 5.1 新增清新简约风格的 `LoginDialog.vue`，支持用户名密码登录、错误提示和登录成功回填状态
- [x] 5.2 新增清新简约风格的 `RegisterDialog.vue`，支持用户名、密码、昵称、邮箱注册和校验反馈
- [x] 5.3 新增 `LoginGuideDialog.vue`，统一展示“登录后即可使用该功能”并引导打开登录或注册弹窗
- [x] 5.4 改造顶部用户区，游客展示登录/注册入口，登录用户展示昵称或用户名、个人中心入口和退出登录入口
- [x] 5.5 新增 `ProfilePage.vue`，展示头像、用户名、昵称、邮箱、注册时间、经验值、等级名称和段位占位信息
- [x] 5.6 配置个人中心路由，并在现有布局中放置符合简约风格的个人中心入口
- [x] 5.7 配置路由守卫或统一入口拦截，游客访问热门面经、AI智能刷题、个人中心时展示登录引导
- [x] 5.8 保持学习路线和建议评论区占位页对游客可访问，不引入登录限制
- [x] 5.9 实现前端退出登录流程，调用后端退出接口后清理 token、用户信息和顶部用户状态

## 6. 人工验证与收尾

- [x] 6.1 使用本地 MySQL 启动后端，确认 Flyway 自动创建 `users` 表
- [x] 6.2 人工验证注册成功后数据库保存密码哈希而非明文
- [x] 6.3 人工验证重复用户名注册返回 `RESOURCE_CONFLICT` 和明确中文提示
- [x] 6.4 人工验证正确密码登录成功、错误密码登录失败并返回 `AUTH_UNAUTHORIZED`
- [x] 6.5 人工验证未登录访问 `/api/v1/users/me` 返回 `AUTH_UNAUTHORIZED`，登录后访问不返回密码哈希
- [x] 6.6 人工验证前端登录成功后顶部展示用户信息，刷新页面后能恢复登录态
- [x] 6.7 人工验证游客访问热门面经、AI智能刷题、个人中心时展示登录引导
- [x] 6.8 人工验证退出登录后 token 和用户信息被清理，受保护入口重新展示登录引导
- [x] 6.9 执行后端编译和前端构建检查，不新增单元测试代码

