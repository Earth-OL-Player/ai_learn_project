# sprintYYYYMM 当期验收文档

版本：v1.0  
日期：YYYY-MM-DD  
迭代定位：填写本期迭代定位

## 1. 项目启动步骤

### 1.1 必要的环境变量和中间件



### 1.2 启动后端

```powershell
cd ai-learn-backend
mvn spring-boot:run
```

启动成功后访问地址：

```text
http://localhost:8080
```

### 1.3 启动前端

```powershell
cd ai-learn-web
npm install
npm run dev
```

启动成功后访问地址：

```text
http://localhost:5173
```

说明：本项目当前要求禁止新增单元测试代码，验收文档只记录构建、接口联调和人工验收步骤。

## 2. 前端页面和功能验收

### 2.1 页面或功能一

验收步骤：

1. 填写用户操作步骤。
2. 填写页面跳转或功能触发方式。

验收点：

- 填写可观察的页面结果。
- 填写关键数据、提示文案或交互结果。
- 填写异常场景或边界场景。

### 2.2 页面或功能二

验收步骤：

1. 填写用户操作步骤。
2. 填写页面跳转或功能触发方式。

验收点：

- 填写可观察的页面结果。
- 填写关键数据、提示文案或交互结果。
- 填写异常场景或边界场景。


## 3. 验收记录表

| 编号 | 验收项 | 验收结果 | 验收人 | 验收时间 | 备注 |
| --- | --- | --- | --- | --- | --- |
| SXX-AXX | 填写验收项 | 待验收 |  |  |  |
| SXX-AXX | 填写验收项 | 待验收 |  |  |  |
| SXX-AXX | 后端构建 | 待验收 |  |  | `mvn -DskipTests package` |
| SXX-AXX | 前端构建 | 待验收 |  |  | `npm run build` |
