# AGENTS.md

## 项目级开发要求

1.在本项目开发过程中，请始终遵守以下要求：

1. 如果新增或修改功能开始依赖 MySQL、Redis、Qdrant 等中间件，必须同步补充中间件说明文档。
2. 中间件说明文档统一提交到 `doc/中间件/` 目录下；如果目录不存在，需要先创建该目录。
3. 文档需要说明该中间件在项目中的用途、推荐版本、本地安装方式、本地启动方式、必要配置项、示例占位符配置、验证方式，以及后续部署到服务器时的注意事项。
4. 开源仓库内不得提交真实密码、Token、密钥、生产连接地址等敏感信息，所有示例配置必须使用占位符，并用中文注释说明真实值应该如何配置。
5. 如果代码中新增了连接中间件所需的环境变量、配置文件字段或启动参数，也必须在 `doc/中间件/` 对应文档中同步更新，保证开发者可以仅根据文档完成本地搭建、启动和联调。
6. 修改完成后，需要在回复中明确说明新增或更新了哪些中间件文档，以及这些文档对应支持哪些本地开发或部署场景。

2.在本项目开发过程中，请始终遵守以下要求：
1. 代码开发过程中，“禁止写单元测试代码”，我会另外安排时间统一补单元测试。
2. 前端界面UI风格，要求清新简约大气！
3. 收到“编写验收文档”“生成验收文档”“整理验收文档”“更新验收文档”等验收文档相关命令时，统一使用 `doc/3.迭代文档/文档模板/验收文档模板.md` 简化模板。
